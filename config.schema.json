{
    "pluginAlias": "pioneerAvr2025",
    "pluginType": "platform",
    "singular": true,
    "headerDisplay": "This is a plugin that integrates your Pioneer AVR as a TV accessory in HomeKit.\n\nTo use the network scan (Multicast), leave `ip` field empty in the plugin configuration.<br>\n\nThis plugin is specifically designed for Pioneer models released before 2017 that use Pioneer Telnet Commands (e.g., VSX-922). It may not be compatible with newer models that use the ISCP protocol (e.g., VSX-LX304). For newer models, please consider using the [homebridge-onkyo-pioneer](https://github.com/nitaybz/homebridge-onkyo-pioneer) plugin.\n\nTo open a telnet port on the receiver or set Network Standby, click here: [http://VSX-923.local/1000/port_number.asp](http://VSX-923.local/1000/port_number.asp).",
    "schema": {
        "title": "Pioneer AVR Plugin",
        "type": "object",
        "properties": {
            "name": {
                "type": "string",
                "title": "Platform Name",
                "description": "The name of the plugin, e.g. shown in the logs.",
                "default": "pioneerAvr2025",
                "placeholder": "pioneerAvr2025",
                "required": true
            },
            "device": {
                "type": "object",
                "title": "Device Configuration (Optional)",
                "expandable": true,
                "expanded": false,
                "properties": {
                    "name": {
                        "type": "string",
                        "title": "Device Name",
                        "description": "Enter the name of the device visible in HomeKit.",
                        "default": "",
                        "placeholder": "VSX-923",
                        "condition": {
                            "functionBody": "return !model.device.name || model.device.name !== '';"
                        }
                    },
                    "ip": {
                        "type": "string",
                        "title": "Device IP Address",
                        "description": "Enter the IP address or the DNS name of the device (e.g., VSX-923.local).",
                        "default": "",
                        "placeholder": "VSX-923.local",
                        "condition": {
                            "functionBody": "return !model.device.name || model.device.name !== '';"
                        }
                    },
                    "port": {
                        "type": "integer",
                        "title": "Device Port",
                        "description": "Enter the port number for the device connection (e.g., 23 or 8102). To open the port, visit: http://VSX-923.local/1000/port_number.asp",
                        "placeholder": 24,
                        "default": 24
                    }
                },
                "required": false,
                "description": "Configure the device parameters manually if needed."
            },
            "maxVolumeSet": {
                "type": "integer",
                "title": "Maximum Volume",
                "description": "The maximum volume level allowed for the AVR.",
                "default": 80,
                "minimum": 0,
                "maximum": 100
            },
            "minVolumeSet": {
                "type": "integer",
                "title": "Minimum Volume",
                "description": "The minimum volume level allowed for the AVR.",
                "default": 30,
                "minimum": 0,
                "maximum": 100
            }
        }
    }
}